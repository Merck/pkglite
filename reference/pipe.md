# Pipe operator

See `magrittr::%>%` for details.

## Usage

``` r
lhs %>% rhs
```

## Value

The evaluation result of `rhs(lhs)`, if `rhs` is a function and `lhs` is
an object. See magrittr::%\>% for more details.
